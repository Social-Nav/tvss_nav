<launch>
  <!--args-->
  <arg name="show_rviz" default="true" />
  <arg name="rviz_file" default="visual_semantic" />

  <!-- load static parameters-->
  <arg name="shared_param_cfg" default="common.yaml" />
	<rosparam command="load" file="$(find tvss_nav)/config/$(arg shared_param_cfg)" />

  <!-- rosbridge -->
  <include file="$(find rosbridge_server)/launch/rosbridge_websocket.launch" />

  <!-- depth to cloud -->
  <!-- <node pkg="nodelet" type="nodelet" name="depth_to_cloud" args="standalone depth_image_proc/point_cloud_xyz">
    <remap from="image_rect" to="/camera/aligned_depth_to_color/image_raw"/>
    <remap from="camera_info" to="/camera/color/camera_info"/>
    <remap from="points" to="/depth_cloud"/>
  </node> -->

  <!-- pointcloud_seg -->
  <include file="$(find tvss_nav)/launch/utils/pointcloud_seg.launch" />

  <!-- goal_projector -->
  <include file="$(find tvss_nav)/launch/utils/goal_projector.launch" />

  <!-- rviz -->
  <include file="$(find tvss_nav)/launch/utils/rviz.launch">
    <arg name="show_rviz" value="$(arg show_rviz)" />
    <arg name="rviz_file" value="$(arg rviz_file)" />
  </include>

</launch>
